package com.studytracker.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS is configured via the CorsConfigurationSource bean in SecurityConfig
}
